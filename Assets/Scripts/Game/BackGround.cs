using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackGround : MonoBehaviour {
    /*
    [SerializeField]
    private GameObject _ground;

    private int num = 5;
    private int num1 = 10;
    private float _gheight = 58f;
    private float _gstartPositionX = 2.56f;
    private float _gstartPositionY = 58f;

    void Start ()
    {
		
	}

	void Update ()
    {
        if( Player._cnt == num )
        {
            num += num1;
        }

    }

    void FixedUpdate()
    {
        if (Player._cnt == num)
        {
            Instantiate(_ground, new Vector3(_gstartPositionX, _gstartPositionY, 0), Quaternion.identity);
            _gstartPositionY += _gheight;

        }
    }
    */
}
